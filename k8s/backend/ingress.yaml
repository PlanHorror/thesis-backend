apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: backend-ingress
  namespace: thesis
  labels:
    app: backend
  annotations:
    # File uploads
    nginx.ingress.kubernetes.io/proxy-body-size: '50m'

    # WebSocket support
    nginx.ingress.kubernetes.io/proxy-http-version: '1.1'
    nginx.ingress.kubernetes.io/proxy-read-timeout: '3600' # 1 hour timeout
    nginx.ingress.kubernetes.io/proxy-send-timeout: '3600'

    # Sticky sessions via cookie (better than IP-based)
    nginx.ingress.kubernetes.io/affinity: 'cookie'
    nginx.ingress.kubernetes.io/session-cookie-name: 'SERVERID'
    nginx.ingress.kubernetes.io/session-cookie-max-age: '3600'
spec:
  ingressClassName: nginx
  rules:
    # For local development (Minikube), add to /etc/hosts:
    # <minikube-ip> api.thesis.local
    - host: api.thesis.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: backend-service
                port:
                  number: 80
    # Allow access without host header (for AWS LB URL direct access)
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: backend-service
                port:
                  number: 80
